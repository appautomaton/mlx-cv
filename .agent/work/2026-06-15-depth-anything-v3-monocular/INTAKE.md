# Intake: Depth Anything V3 — monocular depth (first full task model)

Generated by auto-office-hours on 2026-06-15
Active change: 2026-06-15-depth-anything-v3-monocular
Status: DRAFT
Mode: Builder
Work scale: capability
Work shape: mixed (parity + feature)

## Objective
Port **Depth Anything V3 (monocular)** onto the spine as the first full task model: complete the DINOv2 backbone (real config + convert rules + weight-load + forward parity) that Phase 2 built structurally, add a **DPT dense head**, and produce **`Result.depth` + `depth_conf`** for a single image — with forward parity vs a committed golden fixture.

## Broader Intent
Phase 3 of the foundation-first roadmap: prove the vision spine + a dense head + the load/convert path end-to-end on a real model *before* the heavy VLM (Phase 4 LocateAnything). DA3 is the lowest-difficulty full model and directly consumes the Phase-2 foundation (ViT families, DINOv2, convert engine).

## Shape Context
**Closure Target (parity).** Reference = official PyTorch DA3 (`references/Depth-Anything-3`, `model/da3.py:DepthAnything3Net` + `model/dinov2/` + `model/dpt.py:DPT`), monocular path only. "Closed" = MLX DA3 monocular forward (depth + confidence) matches a committed golden fixture within tolerance on one fixed image; DINOv2 backbone `forward_features` matches its own fixture (the structural Phase-2 DINOv2 gains real weights + parity here). Same fixed-seed tiny-fixture method proven in Phase 1 (DINOv3).

**Feature.** New `heads/dense` (DPT) family; `Result`/`DepthMap` gains a confidence field; DA3 model + convert + processor; depth dense-map inversion via the existing `SpatialTransform` v2 (bilinear).

## Constraints
- `core/` stays mlx-free; mlx only behind the `[mlx]` extra (Phase-1/2 invariant). DPT head + DA3 model live in `heads/`/`models/`; only `Result`/`DepthMap` *types* may touch `core/` (mlx-free).
- Target checkpoint config: **DA3-BASE** (Apache-2.0). Surface the per-checkpoint license (BASE Apache; LARGE/GIANT CC-BY-NC-4.0) in the model card, not as a gate.
- Reuse Phase-2 foundations: DINOv2 backbone, `ViTBackbone`, the `hub/convert.py` rule engine, `SpatialTransform` dense inversion. No new spine abstraction unless a gap is proven.
- No `torch`/`transformers` in `pyproject`; fixtures minted out-of-band (Phase-1 discipline).

## Scope Coverage
- Included: complete DINOv2 (config + convert + weight-load + forward parity); DPT dense head (`heads/dense`); DA3 monocular model + convert + processor; `Result.depth` + `depth_conf`; depth golden fixture + parity; per-checkpoint license note.
- Deferred: camera pose/intrinsics (`cam_enc`/`cam_dec`); multi-view (`reference_view_selector`, multi-image attention); metric-depth variants; DA3-LARGE/GIANT.
- Anti-goals: Gaussian-splatting / 3D reconstruction (`gs_adapter`, `gsdpt`, `dualdpt`); camera estimation; multi-view; any other task model (RF-DETR/SAM/LocateAnything = later phases).
- Needs decision: parity-oracle fidelity (fixed-seed tiny fixture vs real DA3-BASE weights) — **assumption: fixed-seed tiny**, same as Phase 1; confirm at frame. DPT head exact structure + DINOv2-real convert keys — resolved at plan time from the reference.

## Scope Preservation
Intentional decomposition: this is the **monocular depth** slice of DA3. Camera, multi-view, and gaussian paths are explicitly deferred to follow-on phases (recorded above), not dropped. Matches the roadmap Phase-3 exit signal (depth + confidence parity on a fixed image).

## Recommended Approach
Monocular depth + confidence, foundation-reuse first: DINOv2-completion → DPT head → DA3 monocular assembly → parity. Smallest correct outcome that proves the full vision→dense-head→load/convert path and aligns with the roadmap exit signal.

## Key Assumptions and Risks
- DINOv2 real convert may need separate-q/k/v→packed-qkv handling the DINOv3 seed lacks (eng-review R2 carried from Phase 2) — first concrete test of convert-engine generality.
- DA3's DINOv2 may differ from the rf-detr DINOv2 (interpolation/antialias details); confirm the exact reference at plan.
- DPT head reads multiple intermediate ViT layers — exercises `BackboneFeatures.intermediates` (defined in Phase 1, unused until now).

## Next Step
Frame the bounded SPEC for `2026-06-15-depth-anything-v3-monocular` (continue inline into auto-frame).
