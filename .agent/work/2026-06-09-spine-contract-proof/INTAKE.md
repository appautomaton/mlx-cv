# Intake: Spine contract-proof slice + parity harness

Generated by auto-office-hours on 2026-06-09
Active change: 2026-06-09-spine-contract-proof
Status: DRAFT
Mode: Builder
Work scale: capability (Phase 1 of a roadmap-sized plan)
Work shape: mixed (refactor + parity)

## Objective
Define the *minimal* versions of the core spine contracts (`BackboneFeatures`/`FeatureMap` schema, `SpatialTransform` v2 dense inversion, `HeadInput`/`HeadOutput`) plus a golden-fixture harness, and prove them by running a DINOv3 forward pass on MLX that matches the `mlx-image` reference within tolerance — so the foundation is validated on a real model, not on paper, before any task model is built.

## Broader Intent
The foundation-first roadmap for mlx-cv (`.agent/steering/ROADMAP.md`): harden the spine + build the shared blocks before stacking the verified MVP-5 (LocateAnything · DINOv3 · RF-DETR · Depth Anything V3 · SAM 3.1). Evidence: `docs/BUILDING-BLOCKS.md`.

## Mode Context
Builder — the payoff is a modular, parity-tested foundation where adding the Nth model is mostly `convert.py` + `processor.py`. This slice's "whoa" is proving the spine generalizes on a real backbone with a green parity check, not a design doc.

## Shape Context
- Parity / Closure Target: a DINOv3 forward pass on MLX matches `references/mlx-image` within tolerance, mediated by a reusable golden-fixture schema. Fallback oracle: the PyTorch `references/dinov3` if mlx-image outputs are themselves suspect.
- Refactor / Structural Goal: widen the vision-path spine contracts (`VisionBackbone` → `BackboneFeatures`, `SpatialTransform` v2, `Head` I/O) to the *minimum* shape DINOv3 needs — proven, not speculative. `core/` must not need a model-specific edit afterward.

## Constraints
- MLX-native on Apple Silicon; the `mlx` runtime is **not yet installed** → first step is `pip install mlx` (the `[mlx]` extra).
- Inference-only; spine base stays import-light (`mlx` only behind the extra).
- "Fix the spine, not the model" — a model must never force a `core/` edit (ARCHITECTURE §10).

## Scope Coverage
- Included: `BackboneFeatures`/`FeatureMap` schema; `SpatialTransform` v2 (deterministic dense resampling); `HeadInput`/`HeadOutput` shapes; golden-fixture schema + one tiny DINOv3 fixture; DINOv3 MLX forward + parity vs `mlx-image`; install the `mlx` runtime.
- Deferred: full `LanguageBackbone` cache/mask (Phase 4); build-once blocks beyond what DINOv3 needs (Phase 2); all task models (Phases 3–6).
- Anti-goals: building any task model in this slice; `Tracker`/video-memory and `Prompt`-encoder contracts; a broad `ops` package (only what a DINOv3 forward needs).

## Scope Preservation
Intentional decomposition — Phase 1 of the approved 6-phase roadmap; the full intent lives across `ROADMAP.md`. This intake adopts the existing, reviewed decomposition; it does not re-scope it.

## Recommended Approach
Contract-proof *thin* slice validated by DINOv3 forward-parity — chosen over abstract "harden all contracts on paper" to kill the chicken-and-egg risk (Codex review). Proves the contracts on a real model with an existing MLX oracle (`mlx-image`).

## Key Assumptions and Risks
- `mlx` not installed yet → blocks the parity exit until `pip install mlx`.
- `mlx-image` is a small project (~97★) used as the MLX oracle; if its DINOv3 outputs are suspect, fall back to the PyTorch `references/dinov3` reference.
- MoonViT/Qwen2 RoPE/attention variants are explicitly out of scope here (Phase 4).

## Rejected or Deferred
- Abstract "harden all 4 🔴 contracts on paper first" — rejected (chicken-and-egg, per Codex).
- Re-running a full office-hours decomposition — unnecessary; the 6-phase roadmap is already approved and reviewed (this was a lightweight adopt + activate).

## Next Step
Continue into `auto-frame` to write `SPEC.md` with acceptance criteria for Phase 1 (DINOv3 forward-parity within tolerance via the fixture harness; contracts hold with zero `core/` edits needed to carry DINOv3).
