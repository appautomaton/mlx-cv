- PyTorch bicubic interpolation clamps out-of-bounds border sample indices while retaining their cubic weights; MoonViT pos-emb parity depends on that convention. Evidence: `src/mlx_cv/backbones/vision/moonvit/modeling.py`, `tests/test_moonvit_parity.py` (`2026-06-15-locateanything-moonvit-backbone`)
- RF-DETR Nano real checkpoint is not loadable by converter-only fixes: it targets windowed DINOv2, YOLOv5-style projector C2f blocks, two-stage RF-DETR proposal heads, decoder self-attention, decoder norm, and ref-point head that the current local fixture RF-DETR path does not implement. Evidence: `/tmp/mlx-cv-checkpoints/rf-detr-nano.pth`, `.agent/work/2026-06-16-real-checkpoint-bootstrap-rfdetr-nano/PLAN.md` Slice 3 (`2026-06-16-real-checkpoint-bootstrap-rfdetr-nano`)
